# CMake file for exporting common libraries
cmake_minimum_required(VERSION 3.20)

# project(common_libs_tests)
# This is not a project, but the tests are a project.

set(CMAKE_CXX_STANDARD 20) # cpp 2020 standard
set(CMAKE_CXX_STANDARD_REQUIRED True)

# add_subdirectory() # Use to include other libraries

set(BUILD_DIR ${CMAKE_CURRENT_LIST_DIR}/build)

set(COMMON_LIB_SOURCES 
    ${CMAKE_CURRENT_LIST_DIR}/crc/src/*.cpp
    ${CMAKE_CURRENT_LIST_DIR}/crc/src/*.c

    # Autogenerated files:
    # ${BUILD_DIR}/autogen/*.cpp
    # ${BUILD_DIR}/autogen/*.c
)

add_library(common_lib STATIC ${COMMON_LIB_SOURCES})

target_include_directories(
    ${CMAKE_CURRENT_LIST_DIR}/inc/crc/
)

